import express from "express";

export function getGameRouter(
  authenticationRequired: express.Handler
): express.Router {
  const router = express.Router();
  // Get current (check for user game in progress)
  // Show ()
  return router;
}
