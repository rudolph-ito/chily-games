import express from "express";

export function getChallengeRouter(
  authenticationRequired: express.Handler
): express.Router {
  const router = express.Router();
  // List (all or just yours)
  // Create
  // Delete
  // Accept
  // Decline
  return router;
}
