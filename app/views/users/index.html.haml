.title
  Users
  = link_to 'New', [:new, :user]

%table
  %thead
    %tr
      %th Username
      %th Email
      %th Actions

  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= user.email
        %td
          = link_to 'Edit', [:edit, user]
          = link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }
