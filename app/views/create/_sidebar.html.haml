= content_for :sidebar do
  %ul
    %li= link_to "Invariants", [:create, :invariants]
    %li= link_to "Variants", :variants
    - if signed_in? && current_user.can_create?(Variant)
      %li= link_to "New Variant", [:new, :variant]
