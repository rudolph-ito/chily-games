%h1 Variants

%table
  %tr
    %th Name
    %th Description
    %th Board Type

  - @variants.each do |variant|
    %tr
      %td= variant.name
      %td= variant.description
      %td= variant.board_type

      %td
        = link_to 'Show', variant
        - if signed_in? && current_user.can_delete?(variant)
          = link_to 'Destroy', variant, method: :delete, data: { confirm: 'Are you sure?' }

%br

- if signed_in? && current_user.can_create?(Variant)
  = link_to 'New Variant', [:new, :variant]